import React from 'react';
import './styles.css';

const NotFound = (props) => {
    return (
        <div>
            <p className='genericText'>NotFound</p>
        </div>
    );
};

export default NotFound;